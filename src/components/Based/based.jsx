import React from 'react';
import styles from './based.module.css'; // Импортируем CSS как модуль

const Based = () => {
    return (
        <div className={styles.based}> {/* Используем стили как объект */}
            am
        </div>
    );
}

export default Based;